# CodeSprint Deep Review

Generated by a multi-agent deep review (kimi-review + general subagents) on 2025-06-19.
Branch: `chore/hardening-sprint` (branched from `origin/main` @ `b7a3a52`).

## Health Snapshot

| Check | Result |
|---|---|
| Lint | PASS (1 warning in `.pi-harness/` vendored tooling) |
| Build | PASS, 6/6 pages, `/` First Load JS 520 kB |
| `tsc --noEmit` | PASS |
| Tests (Vitest) | PASS, 47 files, 685 tests, 0 failures |
| TODO/FIXME/HACK | None |
| Deps | No vulnerable majors; Next 16 / TS 6 / eslint 10 available |

Overall health: ~8.5/10. Issues below are bugs, tech debt, and feature opportunities.

---

## Phase 1: Critical Bugs

- [x] **1. Gate global shortcuts on overlay state**
  `hooks/useKeyboardShortcuts.ts:264` + `components/AppShell.tsx:72`
  Capture-phase `keydown` listener forwards printable keys without checking for open overlays (Preferences/Shortcuts/Analytics/AI Drill). Typing `r`/`n`/`p`/`a`/`q`/`l`/`Tab`/`Space`/`Enter` while a drawer is open triggers game actions or focus jumps. Also ignore `e.repeat`.

- [x] **2. Tokenizer cache: composite key + LRU cap**
  `lib/tokenizer.ts:180`
  Cache keyed by `content` only, ignoring language, so Python and JS snippets with identical text reuse wrong tokens. Module-level globals `_tokenizeCache`, `_cmCache`, `_pscCache`, `_calcCache`, `_wpCache` grow unbounded (memory leak). Fix: key by `${language}:${content}`, cap with LRU.

- [x] **3. Harden AI key storage**
  `lib/ai/key-storage.ts:13`, `components/AIKeyConfig.tsx:75`
  Provider API keys stored in `localStorage` (XSS/extension exposure) and proxied through `/api/generate`. Move to `sessionStorage`, add a security note in the UI, avoid logging headers server-side.

- [ ] **4. Reset `startTime` on backspace-from-finished**
  `hooks/useTypingEngine.ts:363`
  Backspacing in the `finished` phase sets phase back to `running` but never resets `startTime`, so `elapsedMs` includes the idle pause and produces bogusly low WPM.

- [ ] **5. Fix `LeaderboardModal` accuracy display**
  `components/LeaderboardModal.tsx:74`
  Renders `Math.round(entry.accuracy)}%` but accuracy is stored as a fraction (0.95), so it shows `1%` instead of `95%`. One-line fix: `Math.round(entry.accuracy * 100)`

## Phase 2: High Perf/Correctness

- [ ] **6. Incrementalize CodePanel decorations**
  `components/CodePanel.tsx:421`
  Rebuilds `completedDecorations` by scanning 0..caretIndex on every keystroke, O(n) per key. For long snippets this drops frames. Fix: compute only the delta since the last caret position.

- [ ] **7. Incremental perfect-char scan**
  `hooks/useTypingEngine.ts:480`
  `calculateAndPublishMetrics` loops 0..cursorIndex to recompute `perfectChars` every 200ms tick. Fix: maintain a running counter incrementally.

- [ ] **8. Fix `snippetKey` collisions**
  `components/CodePanel.tsx:63`
  Keys by `${language}-${content.length}-${content.slice(0,16)}`; distinct snippets with the same prefix+length collide, preventing Monaco remount. Fix: use the snippet `id` (or a content hash).

- [ ] **9. `AIDrillPanel` SSR mobile check**
  `components/AIDrillPanel.tsx:106`
  Reads `window.innerWidth` during render, causing a hydration mismatch. Fix: move to `useEffect`/state or a CSS media query.

- [ ] **10. Delete dead auto-advance**
  `hooks/useSessionLifecycle.ts:95`, `components/ResultCard.tsx:418`
  `setAutoAdvance` sets a deadline that is never scheduled; countdown UI in `ResultCard` is orphaned. Decision: delete the dead code (simpler than finishing a half-built feature).

## Phase 3: Tech Debt (deferred)

- [ ] 11. Remove `"use client"` from pure utils (`lib/snippets.ts`, `lib/scoring.ts`)
- [ ] 12. Dedupe theme logic (`lib/preferences.tsx:56` vs `app/theme-init-script.ts:54`)
- [ ] 13. Gate shortcuts on `e.repeat` (folded into item 1)
- [ ] 14. Fix `\r\n` normalization (`useTypingEngine.ts:19`), Tab-past-4-spaces (`:300`), rate-limiter-on-failure (`lib/ai/rate-limiter.ts:72`)
- [ ] 15. Validate imported/stored session enums (`lib/export.ts:192`)
- [ ] 16. `PreferencesDrawer` reset should include AI + indent prefs (`:76`)
- [ ] 17. `framer-motion` `motion()` -> `motion.create()` migration
- [ ] 18. Fix `act(...)` warnings in `useSessionLifecycle.test.ts`

## Phase 4: Tier 1 Features (deferred)

- [ ] F1. Custom / paste-your-own-code snippet (S, must-have)
- [ ] F2. Local mistake-micro-drills, no API key (S-M, must-have)
- [ ] F3. Sound feedback, WebAudio (S)
- [ ] F4. PB pacer line + live target-WPM badge (S)
- [ ] F5. Snippet browser/search, virtualized (S-M)

## Tier 2 Features (deferred)

- [ ] F6. Timed & word-count test modes (M, must-have, Monkeytype parity)
- [ ] F7. Per-key/finger heatmap + slowest-keys (M, typing.io/Keybr parity)
- [ ] F8. Syntax-pattern practice tracks (M, "for engineers" differentiator)
- [ ] F9. Run replay, scrubbable (M, depends on F7 event capture)
- [ ] F10. Goal/target training with live ghost caret (M)

## Tier 3 Features (deferred)

- [ ] F11. Async global daily leaderboard -> realtime multiplayer (L)
- [ ] F12. Personalized adaptive curriculum, "Duolingo for code typing" (L)
- [ ] F13. Tree-sitter semantic typing (L, research-y)
- [ ] F14. Bring-your-own-repo import (L)

---

## Other Notable Findings (not in sprint)

- `app/api/og/route.tsx:22` uses `import.meta.url` for font loading, unreliable in Edge runtime; add a fallback.
- `lib/snippets.ts:808` comment stripping can corrupt regex/template literals; needs a proper lexer.
- `lib/leaderboard.ts` accuracy stored as fraction; sort ignores accuracy tie-breaking.
- `lib/scoring.ts:100` pattern-score cache keyed by array reference, misses on equal-content new arrays.
- Large files approaching/exceeding line guidelines: `lib/snippets.ts` (922), `preferences-core.ts` (521), `CodePanel.tsx` (584), `ResultCard.tsx` (501), `TypingSession.tsx` (479).
- `lib/share-card.ts:110` "Top X%" uses `100 - percentile`, verify semantics.

## Review Workflow

Each sprint item follows: **TDD (failing test -> fix) -> verify (`bun run lint && bun run build && bunx vitest run`) -> `codex review` on diff -> synthesize -> accept or revise -> atomic commit -> check the box.**
