// Twitter reuses the Open Graph card verbatim (summary_large_image is the same
// 1200x630 canvas), so this route is a re-export rather than a second design.
export { default, alt, size, contentType } from "./opengraph-image";
